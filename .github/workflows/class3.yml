name: composite actions custom flow
run-name: this flow is generated by me
on: 
    workflow_dispatch:
        inputs:
          username: 
            default: Singaniya
            required: true
            description: Enter username
            type: string 
          password: 
            default: shettyone
            required: true
            description: Enter password
            type: string 
        
jobs:
    firstjob:
        name: first job i am doing
        runs-on: ubuntu-latest
        steps:
            - name: checkout the code
              uses: actions/checkout@v3
            - name: i am teisng something
              id: kia
              #uses: ./.github/actions/testing
              uses: pradeepshettyh/actrepo@v2
              with:
                username: ${{ inputs.username }}
                password: ${{ inputs.password }}
            - name: some tessing
              run: |
                echo 'the output is ${{ steps.kia.outputs.somedata }}'



              

