name: Pradeep Learning
on: workflow_dispatch
run-name: Something Data
jobs:
    first-job: 
        name: Testing my first job
        runs-on: ubuntu-latest
        strategy:
          matrix:
            os_version: [1,2,3]
            some_version: [4,5]
        steps:
            - name: first step
              id: run-tests
              run: |
                echo 'Hello how are you'
                exit 1
            - name: second step
              if: 
              run: |
                echo 'keka taka ${{ matrix.os_version }} ----- ${{ matrix.some_version }}'                           

              

        




  
  
